<view class="wd-markdown" style="font-size: {{fontSize}}rpx;" class="{{className}}">
  <!-- <rich-text nodes="{{__html}}" /> -->
  <mp-html content="{{__html}}" scroll-table="{{true}}" selectable="{{true}}" bind:imgTap="imgTap"/>
</view>
